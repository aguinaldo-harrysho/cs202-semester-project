#include <vector>
/**
 * @brief This is the Normalizer class
 * 
 */
class Normalizer
{
    public:
    /**
     * @brief Processor that normalizes the data
     * @param data - Samples from the data section of the input file.
     * @return std::vector<float>
     */
    static wav_body process (wav_body);
};