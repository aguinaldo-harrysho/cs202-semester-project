#include <iostream>
#include <fstream>
#include "wav.h"

Wav::Wav(const std::string filename)
{
    wav_header header;
    //std::cout << header.test << std::endl;
    //readHeader(filename);
    //printHeader(filename, header);
}